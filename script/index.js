const userLibrary = [];

function Book(title, author, pages, read, image) {
    //general constructor
    this.title = title;
    this.author = author;
    this.pages = pages;
    this.read = read;
    this.image = image;
};

function addBookToLibrary() {
    //do stuff here
}